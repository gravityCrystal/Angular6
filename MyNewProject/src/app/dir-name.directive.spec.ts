import { DirNameDirective } from './dir-name.directive';

describe('DirNameDirective', () => {
  it('should create an instance', () => {
    const directive = new DirNameDirective();
    expect(directive).toBeTruthy();
  });
});
